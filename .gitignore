# Local Python environment and generated bytecode
.venv/
__pycache__/
*.py[cod]

# Local secrets (keep the configuration example)
.env
.env.*
!.env.example

# Development database, uploads, model weights/archives, and generated assets
db.sqlite3
db.sqlite3-*
/media/
/models/
/staticfiles/
/logs/

# Local tools and operating-system metadata
/.claude/
.DS_Store

# Tests and test cache: excluded from GitHub at the project owner's request
/tests/
/pytest.ini
.pytest_cache/

# Development-only data generators
/apps/academics/management/commands/seed_demo.py
/apps/academics/management/commands/seed_teacher_timetable.py
/apps/face/management/commands/seed_face_enrollments.py

# Git's own metadata is never tracked and needs no ignore rule.
