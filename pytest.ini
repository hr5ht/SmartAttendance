[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests
addopts = -q
markers =
    slow: needs the real face model on disk
filterwarnings =
    ignore:`estimate` is deprecated:FutureWarning
